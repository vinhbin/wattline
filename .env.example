# The app runs with no environment at all — the API serves precomputed JSON
# (data/processed/ -> mocks/) and the pipeline reads/writes local files.
#
# Only for scripts/fetch_acs.py (Census now requires a key — free + instant):
# https://api.census.gov/data/key_signup.html
CENSUS_API_KEY=
